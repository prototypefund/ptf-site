{% set timeline = module.timeline.one() %}

{% set content %}
  {% if timeline|length %}
    {% include "_modules/timeline" with {
      phases: timeline.phases,
      text: timeline.text
    } only %}
  {% endif %}
{% endset %}

{% include "_modules/side-heading" with {
  headline: module.title,
  content: content
} only %}
