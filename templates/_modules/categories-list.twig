{% set categories = categories|default([]) %}
{% set size = size|default("small") %}

{% set textSize = {
  small: "text-body-12 md:text-body-13",
  large: "text-body-12 md:text-body-14"
} %}

<ul class="flex flex-wrap gap-y-0.5">
  {% for category in categories %}
    <li
      class="text-gray-800 uppercase dark:text-gray-600 {{
      textSize[size]
      }} text-gray-600 after:pr-1 after:pl-[5px] after:text-gray-600 after:content-['|'] last:after:hidden dark:after:text-[#575757]"
    >
      {{ category.title }}
    </li>
  {% endfor %}
</ul>
