{% extends "_layouts/layout.twig" %}

{% block content %}
  <div class="text-gray flex h-[50vw] items-center justify-center">
    Homepage header
  </div>

  {% include "_modules/modules" with {
    entry: entry
  } only %}
{% endblock %}
