{% set featured = featured|default(null) %}

<div class="md:grid md:grid-cols-2 md:gap-x-5 md:gap-y-[60px]">
  {% if featured %}
    {{
      featured.render({
        featured: true
      })
    }}
  {% endif %}

  {% for article in articles %}
    {{ article.render() }}
  {% endfor %}
</div>
