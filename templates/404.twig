{% extends "_layouts/layout.twig" %}

{% block content %}
  <article
    class="mx-auto mt-6 max-w-4xl px-2 pb-2 md:px-4 md:pb-4 lg:px-6 lg:pb-6"
  >
    <h1 class="text-head-32 mb-0.5">{{ "404!"|t }}</h1>
    <p class="text-body-16 mb-0.5">
      {{ "Page not found"|t }}
    </p>
    <p>
      <a class="text-link" href="{{ homepage.getUrl() }}">
        {{ "Return to homepage"|t }}
      </a>
    </p>
  </article>
{% endblock %}
